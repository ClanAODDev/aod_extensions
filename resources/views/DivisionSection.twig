<div class="section-sub-section automenu">
    {% if attr['section_title'] is not empty %}
        <h2 id="{{ sectionTitle }}">
            {{ sectionTitle }}
        </h2>
    {% endif %}

    {{ content }}
</div>